{
  "name": "govnft",
  "version": "1.0.0",
  "description": "GovNFT enables vesting of ERC-20 tokens in the form of an ERC-721 token.",
  "devDependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-solidity": "^1.2.0"
  },
  "scripts": {
    "format": "prettier --write 'src/**/*.(sol|json)' 'test/**/*.(sol|json)'",
    "format:check": "prettier --check 'src/**/*.*(sol|json)' 'test/**/*.(sol|json)'"
  }
}
